<!DOCTYPE html>
<html lang="ja">
 <head>
   <meta charset="utf-8">
   <title>javascript test</title>
   <script type="text/javascript">
     <!--
	 /* マウスクリック時の座標取得 */
	 function moveToClick(evt) {
	 evt = (evt)? evt : event;
	 if (evt.shiftKey) {
	 var coords = getPageEventCoords(evt);
	 DHTMLAPI.moveTo("mapArrow",coords.left,coords.top);
	 }
	 }
	 
	 // -->
   </script>   
 </head>
 <body onclick="moveToClick()">
   <form name="F1" action="#">
   </form>
 </body>
</html>
